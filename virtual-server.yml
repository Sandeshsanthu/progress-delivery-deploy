# Code Generated by Sidekick is for learning and experimentation purposes only.
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: progressive-delivery-vs
  namespace: progressive
spec:
  hosts:
    - "*"
  gateways: 
    - default/my-gateway
  http:
    - name: primary
      route:
        - destination:
            host: progressive-delivery.progressive.svc.cluster.local
            subset: stable
            port:
              number: 80 # This matches the 'port' in your Service
          weight: 100
        - destination:
            host: progressive-delivery.progressive.svc.cluster.local
            subset: canary
            port: 
              number: 80 # This matches the 'port' in your Service
          weight: 0

